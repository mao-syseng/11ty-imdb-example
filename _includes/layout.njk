---
title: 11ty imdb test
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <meta
      name="description"
      content="Explore the IMDB Top 950 movies. Browse, search, and find detailed information on your favorite films, from classics to modern hits."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="preload"
      href="https://fonts.googleapis.com/css2?family=Bytesized&display=swap"
      as="style"
      onload="this.rel='stylesheet'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=Bytesized&display=swap"
        rel="stylesheet"
      />
    </noscript>
    <style>
      :root {
        --bg-color: #17141c;
        --text-color: #a692b0;
        --input-bg: #a692b0;
        --input-text: #17141c;
      }

      body {
        font-family: "Bytesized", sans-serif;
        font-weight: 400;
        font-style: normal;
        font-size: 24px;
        background-color: var(--bg-color);
        color: var(--text-color);
        margin: 32px;
        display: flex;
        gap: 16px;
      }

      li {
        list-style: none;
      }

      a {
        color: var(--text-color);
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      aside {
        padding-right: 16px;
        border-right: 2px solid #a692b0;
        flex: 1;
      }
      main {
        flex: 3;
      }
      input {
        background-color: var(--input-bg);
        color: var(--input-text);
        font: inherit;
      }
    </style>
    <title>{{ title }}</title>
  </head>
  <body>
    <aside>
      {% for movie in imdb %}
      <li>
        <a href="/movies/{{ movie.Title | slugify }}{{ movie.Rank }}/">
          {{ movie.Rank }}. {{ movie.Title }}
        </a>
      </li>
      {% endfor %}
    </aside>
    <main>
      {{ content | safe }}
    </main>
  </body>
</html>
